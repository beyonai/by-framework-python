# Written by install-map.sh — the single source of truth for map layout.
# Guards source this before their defaults: env → .map.conf → default.
: "${MAP_KEY_FILES:=docs/architecture/KEY_FILES.md}"
: "${MAP_REFERENCE_DOCS:=docs/architecture/KEY_FILES.md docs/TESTING.md docs/architecture/GUARD_AUTHORING.md}"
: "${MAP_SOURCE_GLOBS:=src/ libs/}"
: "${MAP_CLAUDE_MAX_BYTES:=40000}"
: "${MAP_HOOK_SETTINGS:=.claude/settings.json}"
: "${MAP_RESIDENT_FILE:=CLAUDE.md}"
: "${MAP_AGENTS:=claude,codex}"
